mainClassName = 'com.zmunm.narvcorp.sample.mqtt.publisher.InitKt'
version = '1.0'

shadowJar {
    archiveName = "SamplePublisher.jar"
    destinationDir = file("archive")
}

dependencies{
    implementation 'commons-daemon:commons-daemon:1.1.0',
            'org.yaml:snakeyaml:1.19'
}
